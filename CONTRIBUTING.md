# Contributing

Contributions should preserve three rules:

1. Keep `/pm-skills-select` as the default router.
2. Add new skills to `catalog/skills.json` and generate matching plugin files.
3. Every skill must coach the PM, not just output an artifact.
